.venv/
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.mypy_cache/

# User-local config (do not check in)
config.toml
filters.json

# Local-only burn-test audio fixture (contains private operational content;
# referenced by tests/test_burn_parakeet_normalize.py which skips gracefully
# when absent on fresh clones)
tests/fixtures/audio/long_42s_chunking.wav

# Nix build output symlink
result
result-*

# Claude Code user-local settings + worktrees
.claude/settings.local.json
.claude/worktrees/

*.local